# create a dev conda environment with: `make create_env` or `conda env create -f environment.yml`
name: bioptim
channels:
  - conda-forge
dependencies:
  - biorbd >=1.9.5
  - python
  - numpy
  - matplotlib
  - scipy
  - pyomeca
  - vtk
  - pyqt
  - pandas
  - eigen
  - casadi
  - rbdl
  - packaging
  - pytest
  - setuptools
